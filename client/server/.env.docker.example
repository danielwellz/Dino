# Server Docker environment template
# Copy to server/.env.docker and adjust secrets before running `docker compose up`.

PORT=8000
DATABASE_URL=postgresql://postgres:postgres@db:5432/pm_app?schema=public

JWT_ACCESS_TOKEN_SECRET=change-me-access
JWT_REFRESH_TOKEN_SECRET=change-me-refresh

STATUS=production
FRONTEND_URL=http://localhost:3000

# Optional: mailer / integrations
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
